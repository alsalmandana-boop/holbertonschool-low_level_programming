# Doubly Linked Lists

This project covers the implementation and use of doubly linked lists in C.

## Task 0: Print list

The `print_dlistint` function prints all elements of a `dlistint_t`
list and returns the number of nodes.§
